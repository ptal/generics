package ds;

public class Algorithm {
    /**
     * Sort a list of comparable elements.
     * The type of elements contained in the list must thus be a class which implements the interface <code>Comparable</code>.
     * @param <T> type of elements contained in the list
     * @param list given list to sort
     */
    public static <T extends Comparable<T>> void sort(List<T> list) {
        
    }

    public static void main(String[] args) {
        
    }

}
